'use client';

import axios from 'axios';
import Link from 'next/link';
import React, { useEffect, useState } from 'react';

export default function ResetPasswordPage() {
  // Ask for the email for reset
  // Trigger API to send an email to change the password
  // Show a text that they should check their email
}
